name: odoo-18

on:
  workflow_dispatch:
  workflow_call:
  pull_request:
    types:
      - opened
      - synchronize
  push:
    branches:
      - '18*'

jobs:
  all:
    uses: tawasta/github-workflows/.github/workflows/odoo-18.yml@main
    secrets: inherit

